'use client'

export default function ErrorPage() {
  return (
    <div>
  <p>it seems like you are not registered as a user. sign up first</p>
  <p>add a button to go back to login page</p>
  </div>
  )

}